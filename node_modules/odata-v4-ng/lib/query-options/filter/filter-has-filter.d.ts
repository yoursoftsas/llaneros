import { Filter } from './filter';
export declare abstract class FilterHasFilter {
    protected filter: Filter | Filter[];
    constructor(filter: Filter | Filter[]);
    getFilter(): Filter | Filter[];
}
