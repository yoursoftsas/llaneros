import { Filter } from './filter';
import { FilterHasFilter } from './filter-has-filter';
export declare enum LambdaOperator {
    ANY = 0,
    ALL = 1,
}
export declare enum LambdaCollection {
    PROPERTY_COLLECTION = 0,
    ENTITY_SET = 1,
}
export declare class FilterLambda extends FilterHasFilter implements Filter {
    private lambdaCollection;
    private propertyOrEntitySet;
    private lambdaOperator;
    constructor(lambdaCollection: LambdaCollection, propertyOrEntitySet: string, lambdaOperator: LambdaOperator, filter: Filter);
    toString(): string;
    isEmpty(): boolean;
    protected checkProperty(propertyOrEntitySet: string, filter: Filter | Filter[]): void;
    protected replaceProperty(filter: Filter | Filter[]): void;
}
