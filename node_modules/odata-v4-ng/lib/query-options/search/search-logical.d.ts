import { OperatorLogical } from '../operator';
import { Search } from './search';
export declare class SearchLogical extends Search {
    private values;
    private operator;
    constructor(values: Search[], operator: OperatorLogical);
    toString(): string;
    isEmpty(): boolean;
}
